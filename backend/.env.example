# Database Configuration
DATABASE_URL="postgresql://username:password@localhost:5432/dayflow?schema=public"

# JWT Configuration
JWT_SECRET="your-256-bit-secret-key-here-replace-with-cryptographically-random-string"

# Server Configuration
PORT=3000
NODE_ENV="development"

# CORS Configuration (production only)
ALLOWED_ORIGINS="https://app.dayflow.com,https://dayflow.com"
